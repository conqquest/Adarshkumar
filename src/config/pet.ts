export const petConfig = {
  enabled: true,
  name: "Mowgli",
  wakeText: "Wake up Mowgli",
  /** Center point in the navbar (px). h-14 header → y ≈ 28 */
  spawn: { x: 40, y: 28 },
  persistPosition: false,
};
